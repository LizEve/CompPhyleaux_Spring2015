
Simple commit if you have folder already git set up:
$ git status
#tells you what has been edited and what hasn’t been added yet
$ git add “filename”
#if you need to add a filename do this, if you are okay with updating everything that you have changed at once do…
$ git commit -a
#this will open a vim file
#go to a hashtag and type any letter. delete the hashtag and write your commit message in “”
#press esc key. then type :wq 
#this will save your file and quit vim. 
#push your files!
$ git push
 




How to set up a repo from computer (you must make the directory on github first, there is no way to push a new directory onto github)
https://help.github.com/articles/adding-an-existing-project-to-github-using-the-command-line/

#navigate into local folder you want to push to your remote repository
$ git init
$ git add .
$ git commit -a -m 'first commit'
$ git remote add 'git@github.com:LizEve/PopGen_Fall2014.git'
$ git remote add origin 'git@github.com:LizEve/PopGen_Fall2014.git'
$ git remote -v


Websites:

https://gist.github.com/CristinaSolana/1885435
 